'use client';

import { type ReactElement } from 'react';

const About = (): ReactElement => {
  return (
    <div className="w-full p-20 relative z-[2]">
      <h3 className="font-black text-6xl text-center">About</h3>
    </div>
  );
};

export default About;
